#include<bits/stdc++.h>

using namespace std ;

class Node{
    public:
        int data;
        Node* next;

    Node(int value)
    {
        this->next = data;
        this->next = NULL;
    }
};

int main()
{
    return 0;
}